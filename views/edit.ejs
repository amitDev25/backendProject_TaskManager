<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>

</head>
<body>

    <div class="main w-full min-h-screen bg-zinc-900 p-10 text-white">
        <a class="text-blue-500 inline-block mb-10 hover:text-blue-400" href="/">Go Back To Home</a>

        <form action="/edit" method="post">
            <input class="block w-2/4 px-1 py-2 bg-zinc-900 rounded-md outline-none text-3xl" type="text" placeholder="Enter the title.." name="previousName" value="<%= fileName.replace('.txt', '') %>" readonly >
            <input class="block w-2/4 mt-3 px-5 py-2 bg-zinc-800 resize-none rounded-md outline-none" type="text" placeholder="Enter New File Name" value="<%= fileName.replace('.txt', '') %>"  name="newName">
            <textarea class="block w-2/4 mt-3 px-5 py-2 bg-zinc-800 resize-none rounded-md outline-none min-h-52" placeholder="Write the details of your task here" name="newDetails" ><%= fileData %></textarea>
            <input class="mt-3 bg-blue-800 hover:bg-blue-700 px-5 py-2 rounded-md cursor-pointer" type="submit" value="Update Task">
        </form>


    </div>
    
</body>
</html>